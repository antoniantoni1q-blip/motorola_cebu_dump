name: Makefile CI

on:
  push:
    branches: [ "RZCS31.Q2-57-12-14" ]
  pull_request:
    branches: [ "RZCS31.Q2-57-12-14" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
